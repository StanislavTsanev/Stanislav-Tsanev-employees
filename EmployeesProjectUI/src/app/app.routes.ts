import { Routes } from '@angular/router';
import { EmployeeUploadComponent } from './employee-upload/employee-upload.component';

export const routes: Routes = [
  { path: '', component: EmployeeUploadComponent },
];